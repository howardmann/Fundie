<h1>All Projects</h1>
<% if @current_user %>
  <%= link_to "New project", new_project_path, :class => "btn" %>
<% else %>
  <%= link_to "Join the fun, start a new Project", new_project_path, :class => "btn" %>
<% end %>
<hr>
<%= render "shared/project", object: @projects %>
